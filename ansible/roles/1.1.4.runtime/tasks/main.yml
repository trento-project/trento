---

- name: 1.1.4.runtime
  shell: 'corosync-cmapctl | grep "runtime.config.totem.join (u32) = " | sed "s/^.*= //"'
  register: result
  changed_when: false

- include_role:
    name: "set-results"
  vars:
    fact_name: "1.1.4.runtime"
    fact_value: "{{ result.stdout }}"

