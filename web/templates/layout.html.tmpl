{{- /*gotype: github.com/SUSE/console-for-sap-applications/web.LayoutData*/ -}}
<!doctype html>
<html>
  {{ template "header" . }}
  <body>
    {{ block "errors" . }}{{ end}}
    {{ template "sidebar" . }}
    <section class="content">
      {{ template "submenu" .Submenu }}
      <div class="container">
        {{ template "content" .Content }}
      </div>
    </section>
    {{ template "footer" . }}
  </body>
</html>
