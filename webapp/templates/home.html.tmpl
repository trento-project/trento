{{define "indexPage"}}
<!doctype html>
<html>
  {{ template "header" . }}
  <body>
    {{ template "sidebar" . }}
    <section class="content">
      {{ template "submenu" . }}
      <div class="container">
        <h1>{{ .Title }}</h1>
         <p>This is the home page</p>
      </div>
    </section>
    {{ template "footer"}}
  </body>
</html>
{{end}}
